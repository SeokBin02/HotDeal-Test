spring:
  datasource:
    master:
      hikari:
        driver-class-name: org.h2.Driver
        jdbc-url: jdbc:h2:mem:db;MODE=MYSQL;
        username: sa
        password:
    slave1:
      hikari:
        driver-class-name: org.h2.Driver
        jdbc-url: jdbc:h2:mem:db;MODE=MYSQL;
        username: sa
        password:
    slave2:
      hikari:
        driver-class-name: org.h2.Driver
        jdbc-url: jdbc:h2:mem:db;MODE=MYSQL;
        username: sa
        password: